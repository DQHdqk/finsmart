server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/finsmart?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1265631126@Djj
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

  jackson:
      date-format: yyyy-MM-dd
      serialization:
        write-dates-as-timestamps: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs
    config-url: /v3/api-docs/swagger-config

finsmart:
  jwt:
    secret: finsmart_secret_key_2024_dengjiajia
    expire: 604800
  ai:
    api-key: ${ZHIPU_API_KEY}
    url: https://open.bigmodel.cn/api/paas/v4/chat/completions
    model: glm-4-flash





